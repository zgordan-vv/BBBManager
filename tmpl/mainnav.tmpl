<section class="container-fluid panel-horiz">
<section ng-controller="NavLogged as nlc" class="col-xs-offset-1">
	<ul>
		<li ng-repeat="item in nlc.items"><a href="{{item.link}}">{{item.name}}</a></li>
	</ul>
</section>
</section>
